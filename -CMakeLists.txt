cmake_minimum_required(VERSION 3.16)

project(JeuTBB LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Dossier des headers
include_directories(include)

# Dossier de sortie pour tous les exécutables
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_SOURCE_DIR}/bin)

add_executable(executable.out
    src/main.cpp
    src/Game.cpp
    src/Ball.cpp
    src/Sender.cpp
    src/Level.cpp
)

target_include_directories(executable.out PRIVATE /home/ismae/raylib/src)

add_executable(executable.exe
    src/main.cpp
    src/Game.cpp
    src/Ball.cpp
    src/Sender.cpp
    src/Level.cpp
)

target_include_directories(executable.exe PRIVATE /home/ismae/raylib/src)

set(RAYLIB_WIN_PATH /home/ismae/TBB/Total-Brick-Breaker/raylib)
target_link_libraries(executable.exe PRIVATE ${RAYLIB_WIN_PATH}/libraylib.a)